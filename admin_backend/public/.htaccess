RewriteEngine On

# Allow direct access to existing files/directories
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Route everything else (e.g., /admin-api/...) to index.php
RewriteRule ^ index.php [QSA,L]
